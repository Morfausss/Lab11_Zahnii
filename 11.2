#include <iostream>
#include <cstring>
using namespace std;

int main() {
    const int MAX_LENGTH = 100;
    
    char *a, *b, *c;
    
    a = new char[MAX_LENGTH];
    b = new char[MAX_LENGTH];
    c = new char[MAX_LENGTH];
    
    cout << "Enter string for a: ";
    cin.getline(a, MAX_LENGTH);

    cout << "Enter string for b: ";
    cin.getline(b, MAX_LENGTH);

    cout << "Enter string for c: ";
    cin.getline(c, MAX_LENGTH);
    
    cout << "\nYou entered:\n";
    cout << "a: " << a << endl;
    cout << "b: " << b << endl;
    cout << "c: " << c << endl;
    
    delete[] a;
    delete[] b;
    delete[] c;

    return 0;
}
